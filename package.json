{
  "name": "project_exam3",
  "version": "1.1.2",
  "description": "project exam front end development",
  "main": "index.js",
  "scripts": {
    "build": "sass src/scss:dist/css",
    "watch": "sass --watch src/scss:dist/css & live-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --w ./**/*.{mjs,scss,html,md}",
    "lint": "eslint src/**/*.mjs",
    "lint-fix": "eslint src/**/*.mjs --cache --fix",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sabaFitwi/project-exam3.git"
  },
  "keywords": [
    "Auction",
    "bid",
    "sell"
  ],
  "author": "saba samuel",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.3",
    "bootstrap-icons": "^1.10.2",
    "get-hidden-height": "^1.0.1"
  },
  "devDependencies": {
    "eslint": "^8.28.0",
    "husky": "^8.0.2",
    "lint-staged": "^13.1.0",
    "live-server": "^1.2.2",
    "prettier": "^2.7.1",
    "sass": "^1.56.2"
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix",
    "*.{js,scss,html,md}": "prettier --write"
  }
}
